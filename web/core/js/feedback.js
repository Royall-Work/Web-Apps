export function attachFeedback(api){let timer;api.toast=function(text,type='success',ms=1600){let t=document.querySelector('.royall-toast');if(!t){t=document.createElement('div');t.className='royall-toast';document.body.appendChild(t)}t.className='royall-toast '+type;t.textContent=text||'';requestAnimationFrame(()=>t.classList.add('show'));clearTimeout(timer);timer=setTimeout(()=>t.classList.remove('show'),ms)};api.successToast=(t,m)=>api.toast(t||'Done','success',m);api.warningToast=(t,m)=>api.toast(t||'Warning','warning',m);api.errorToast=(t,m)=>api.toast(t||'Error','error',m);api.copy=async text=>{const v=String(text??'');try{await navigator.clipboard.writeText(v)}catch{const x=document.createElement('textarea');x.value=v;x.style.position='fixed';x.style.opacity='0';document.body.appendChild(x);x.focus();x.select();document.execCommand('copy');x.remove()}api.successToast('Copied')};api.paste=async()=>{try{return await navigator.clipboard.readText()}catch{api.errorToast('Clipboard unavailable');return''}};api.download=function(filename,text,type='text/plain;charset=utf-8'){const blob=new Blob([String(text??'')],{type}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=filename||'download.txt';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),800)};api.downloadNamed=function(o,text,type){api.prompt({icon:'⬇️',title:o.title||'Download',text:o.text||'Enter a file name.',placeholder:o.placeholder||'File name',value:o.value||'download',gravity:'top'},name=>{name=(name||'download').trim();if(!name)return api.errorToast('File name required');const ext=o.extension||'';if(ext&&!name.toLowerCase().endsWith(ext.toLowerCase()))name+=ext;api.download(name,text,type)})}}
